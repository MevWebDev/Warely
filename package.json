{
  "name": "warely",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "docker:dev": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml --env-file .env.development up --build",
    "docker:dev:detached": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml --env-file .env.development up -d --build",
    "docker:prod": "docker-compose -f docker-compose.yml -f docker-compose.prod.yml --env-file .env.production up -d --build",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "docker:clean": "docker-compose down -v --remove-orphans",
    "db:up": "docker-compose up -d postgres mongodb redis",
    "db:down": "docker-compose stop postgres mongodb redis",
    "db:logs": "docker-compose logs -f postgres mongodb redis",
    "dev:local": "pnpm db:up && pnpm -r --parallel dev",
    "dev:docker": "pnpm docker:dev",
    "prod:docker": "pnpm docker:prod"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
